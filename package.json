{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1",
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0"
  },
  "lint-staged": {
    "frontend/**/*.{js,ts,tsx,jsx}": [
      "eslint --fix"
    ],
    "backend/**/*.{js,ts}": [
      "eslint --fix"
    ]
  },
  "scripts": {
    "prepare": "husky"
  }
}
